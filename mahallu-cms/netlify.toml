[build]
  base = "mahallu-cms"
  command = "npm run build"
  publish = "dist"

# SPA Redirects (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
